{
  "scripts": {
    "lint": "prettier --check src && eslint src",
    "test": "jest",
    "ci": "npm run lint && npm run test"
  },
  "devDependencies": {
    "eslint": "^8.29.0",
    "eslint-plugin-react": "^7.31.10",
    "prettier": "^2.8.0"
  }
}
